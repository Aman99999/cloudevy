# Cloudevy Production Environment Variables
# Copy this file to .env on your server: cp env.production.example .env
# Then update with your secure values

# Database Password
DB_PASSWORD=CHANGE_ME_secure_db_password_2024

# InfluxDB Credentials
INFLUX_PASSWORD=CHANGE_ME_influx_password_2024
INFLUX_TOKEN=CHANGE_ME_influx_token_2024

# Backend Security Keys
# Generate JWT Secret: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_jwt_secret_minimum_32_characters

# Generate Encryption Key (must be exactly 32 characters): openssl rand -hex 16
ENCRYPTION_KEY=CHANGE_ME_32_char_encryption_key

# NOTE: URLs are auto-detected based on NODE_ENV
# Production (NODE_ENV=production):
#   - CORS: https://cloudevy.in
#   - Frontend API: Auto-detected from hostname

