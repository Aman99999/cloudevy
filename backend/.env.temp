# Local Development Database (Direct Connection for Migrations)
DATABASE_URL="postgresql://cloudevy:cloudevy_dev_password@localhost:5433/cloudevy?schema=public"

# Keep your other environment variables below this line
